interface:
  display_name: "UnityMCP"
  short_description: "Use live Unity project tools proactively"
  default_prompt: "Use $unity-mcp to inspect the live Unity project and complete this Unity task."

policy:
  allow_implicit_invocation: true
